{
  "name": "@harpe/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Harpe engine + CLI (TypeScript), built on @harpe/core. Ports the Python `harpe` engine: download flows (video/audio/image/page/art/fetch), federated museum search, reverse-image, the native-messaging host and its installer.",
  "bin": {
    "harpe": "./dist/cli.js",
    "grab": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@harpe/core": "workspace:*",
    "node-html-parser": "^6.1.13"
  }
}
