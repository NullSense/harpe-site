{
  "name": "@harpe/mcp",
  "version": "0.0.0",
  "private": true,
  "description": "Model Context Protocol server for Harpe — federated + dump-backed art search and the Wikidata knowledge graph (search_art, artist, subject, resolve, item) over @harpe/sources. Zero-config with the public HF dataset.",
  "type": "module",
  "bin": {
    "harpe-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@harpe/core": "workspace:*",
    "@harpe/sources": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "engines": {
    "node": ">=22"
  }
}
